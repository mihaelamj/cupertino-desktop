/// Namespace for the feature layer. Each feature module adds its own
/// sub-namespace (Search, FrameworkBrowser, DocReader, SampleBrowser) holding an
/// `@Observable` view model that both UI packages bind to.
public enum Feature {}
