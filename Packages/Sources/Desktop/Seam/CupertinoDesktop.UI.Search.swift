import DesktopModels

public extension CupertinoDesktop.UI {
    /// Seam for the search screen. The AppKit and SwiftUI packages each provide a
    /// `Providing` conformer; consumers use them through this namespace identically.
    enum Search {}
}
