/// Root namespace for every Cupertino Desktop type across all modules.
///
/// This file owns the root anchor only. Each module contributes concrete types
/// and its own sub-namespace anchor through extensions on `CupertinoDesktop`,
/// mirroring the folder layout (docs/rules/code-style.md).
public enum CupertinoDesktop {}
