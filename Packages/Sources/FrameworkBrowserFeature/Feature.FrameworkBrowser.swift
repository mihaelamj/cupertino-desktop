import AppCore

public extension Feature {
    /// Framework browser: the sidebar of frameworks from `list_frameworks` and
    /// drill-down into a framework's pages. First real backend call in milestone
    /// M1, views in milestone M2.
    enum FrameworkBrowser {}
}
