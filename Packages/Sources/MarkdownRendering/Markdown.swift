/// Converts the markdown and text strings the server returns into display models
/// for each UI framework (AttributedString for SwiftUI, NSAttributedString for
/// AppKit). Implemented in milestone M2.
public enum Markdown {}
