import DesktopCore

extension Feature {
    /// Search feature: query box, scopes (docs / samples / symbols), and result
    /// list. View model and views are added in milestone M3.
    public enum Search {}
}
