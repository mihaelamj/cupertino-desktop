/// Namespace anchor for the UI-test page objects (the Page Object Model). Concrete pages
/// (`Page.Base`, `Page.FrameworkBrowser`, `Page.Search`) live one per file under this
/// enum, mirroring the module. They drive the SwiftUI, AppKit, and UIKit apps through one
/// set of accessibility identifiers (`UI.AccessibilityID`).
public enum Page {}
